import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'AILMS — Extended Product Showcase (60s+)',
  description: 'Extended cinematic demo with live and illustrative product visuals for leadership.',
}

export default function ShowcaseLayout({ children }: { children: React.ReactNode }) {
  return children
}
