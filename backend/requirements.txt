fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
httpx>=0.27.0
pymupdf>=1.24.0
pillow>=10.0.0
# Optional heavy OCR stack (CPU). Install when the host Python/OS supports it:
#   pip install paddlepaddle paddleocr
# OCR_ENGINE=auto falls back to pymupdf_text (PDF embedded text) if paddleocr is missing.
